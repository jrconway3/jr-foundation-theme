<li class="game-row">
  <a class="game-row-link" href="{{ game.link }}">
    <span class="game-row-icon" aria-hidden="true">🎮</span>
    <span class="game-row-name">{{ game.title }}</span>
    <span class="game-row-count">{{ game.count }}</span>
  </a>
</li>
